# Design QA

最終確認日: 2026-08-13

この文書は、現行 UI の再現可能な確認基準を記録します。個人環境の絶対パスや一時的なスクリーンショットは、別の環境で参照できず陳腐化しやすいため記載しません。

## 確認対象

- 実装: `app/pages/index.vue` と配下のゲーム用コンポーネント
- 画面状態: 開始前、回答前、正解後、不正解後、中断確認、10問完走、3連続不正解による終了
- 画面幅: 320px、390px 前後、PC 幅
- 入力: タッチ、マウス、Tab / Shift+Tab、数字キー、Enter、Escape

## 現行デザイン基準

- オフホワイト、翡翠色、黒、淡い灰、琥珀色を中心に、単色面、明確な境界線、控えめな影で構成します。
- 装飾目的のグラデーションや `999px` のフルピル形状は使いません。
- 出題語、選択肢、進行情報、補助情報の順に視覚的な強弱を付けます。
- 操作領域は44px以上を保ち、320px幅でも横方向へはみ出さないようにします。
- スマホは1列、PC の選択肢は2列とし、PC だけ数字キーに対応する番号を表示します。
- `prefers-reduced-motion` では不要なアニメーションを抑えます。
- PC 幅の開始画面では、ルート説明と10問トラックの直後に開始ボタンを置き、記録詳細より先に主操作を視認できます。
- PC 幅の回答後は、結果帯と `次の問題` を同じ視野に置き、回答結果から次の操作へ移りやすくします。

## 主要導線

1. 開始画面ではレベルを選び、スマホでは記録詳細より先に `ゲームを始める` へ到達できること。
2. 問題画面では進行、スコア、連続数、`終了まで` の残り回数、繁体字、発音、4択を追えること。
3. 正解後は得点と次のボーナス条件、不正解後は正解と終了までの残り回数を優先表示すること。
4. スマホの回答後は、選択した答えと正解以外の選択肢を隠して縦幅を節約すること。
5. `中断` は確認ダイアログを開き、`続ける` と `中断する` を明確に選べること。
6. 終了時は問題カードを畳み、最後の繁体字、正解、必要なら選択した誤答を結果カードへ残すこと。
7. 完走時は `次の10語へ`、3連続不正解時は `この10語に再挑戦` と `トップへ戻る` を、スマホの最初の画面内で操作できること。
8. 主要遷移後はページ上部へ戻り、次に操作すべき要素が分かること。

## アクセシビリティ

- キーボードのフォーカス順が視覚順と一致し、`focus-visible` が十分に見えること。
- 数字キーで選択肢を回答でき、開始前または回答後は Enter で主要操作を実行できること。
- 中断確認は `alertdialog` として通知され、Escape で閉じられること。
- 正誤結果とゲーム終了がライブリージョン経由で支援技術へ伝わること。
- 色だけに依存せず、文言、境界、アイコンを併用して状態を示すこと。
- 音声合成や効果音が使えない環境でも、表示と操作だけでゲームを完了できること。

## 検証手順

```bash
npm run lint
npm run test:unit
npm run test:e2e
npm run typecheck
npm run build
```

Playwright は開始、回答、正誤表示、中断、完走、途中終了、再挑戦、トップ復帰、レスポンシブ表示を確認します。ブラウザで目視確認する場合は、上記の画面状態と幅に加え、コンソールの error / warning も確認します。

## 判定

2026-08-13 時点の実装について、開始前・回答前・正解後・不正解後・ゲーム終了を PC / スマホ幅で確認し、未解決の P0 / P1 / P2 デザイン問題はありません。今後 UI を変更した場合は、固定のテスト件数や一時ファイル名を追記するのではなく、確認日、対象状態、再現可能な手順を更新してください。
